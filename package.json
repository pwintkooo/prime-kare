{
  "name": "prime-kare",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "dotnet watch --project backend/PrimeKare.Api"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
